//! Compatibility re-export for canonical morphosyntax payload helpers now
//! defined in `talkbank-transform`.

pub use talkbank_transform::morphosyntax::{
    PayloadCollection, collect_payloads, declared_languages,
};
