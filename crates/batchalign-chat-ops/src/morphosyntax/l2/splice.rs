//! Compatibility re-exports for canonical L2 splice helpers now defined in
//! `talkbank-transform`.

pub use talkbank_transform::morphosyntax::l2::{
    SpliceOutcome, apply_l2_fallback, splice_l2_into_chat,
};
